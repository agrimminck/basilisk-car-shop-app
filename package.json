{
  "name": "basilisk-car-shop-app",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:reset": "psql $DATABASE_URL -f supabase/migrations/0001_schema.sql && psql $DATABASE_URL -f supabase/migrations/0002_seed.sql"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@10.33.2",
  "pnpm": {
    "onlyBuiltDependencies": ["@serialport/bindings-cpp", "esbuild", "sharp"]
  }
}
